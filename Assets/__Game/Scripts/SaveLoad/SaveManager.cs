using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveManager 
{
    public static void Save()
    {
        // Convert the GameData to bytes and save using Easy Save
      //  ES3.Save("yourSaveKey", value);
    }
    public static void Load()
    {
        // Load bytes from Easy Save and convert them back to GameData
      //  ES3.Load("yourSaveKey");
       // GameManager.Instance.GameData = 
    }

}
